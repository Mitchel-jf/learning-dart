// Write a function called printMessage that takes a required
//parameter message and an optional named parameter times.

// The function should print the message the specified number of times
//(defaulting to 1 if not provided).

void main() {
  printMessage("Hello guys", times: 500);
}

void printMessage(String message, {int times = 1}) {
  for (int i = 0; true; i ) print(message);
}
